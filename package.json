{
  "name": "install-llvm-action",
  "version": "1.0.0",
  "description": "GitHub Action to install LLVM and Clang",
  "main": "src/index.js",
  "scripts": {
    "build": "ncc build src/index.js -o dist",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [
    "github-action",
    "llvm",
    "clang",
    "install"
  ],
  "author": "Tap Lang Team",
  "license": "MIT",
  "dependencies": {
    "@actions/core": "^1.10.1",
    "@actions/exec": "^1.1.1",
    "@actions/io": "^1.1.3"
  },
  "devDependencies": {
    "@vercel/ncc": "^0.38.1"
  }
}
